apiVersion: apps/v1
kind: Deployment
metadata:
  name: fastapi-ml-stable  
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1   
      maxSurge: 1         
  selector:
    matchLabels:
      app: fastapi-ml   
      component: inference-api
      track: stable
  template:
    metadata:
      labels:
        app: fastapi-ml   
        component: inference-api
        version: week8-stable
        track: stable
    spec:
      containers:
        - name: fastapi-ml   
          image: fastapi-ml-example:week8   
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          env:
            - name: MODEL_URI
              value: "file:///app/deployment_mlruns/served_model"
            - name: MODEL_STAGE
              value: "production"
            - name: APP_VERSION
              value: "week8-stable"
            - name: RELEASE_TRACK
              value: "stable"
            - name: MODEL_PATH
              value: "/app/models/lr_model.joblib"
            - name: IMAGE_TAG
              value: "stable"
            - name: MODEL_VERSION
              value: "stable-model-v1"
          resources:
            requests:
              cpu: "250m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
              
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 2
            failureThreshold: 3

          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3